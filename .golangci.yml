version: "2"

run:
  timeout: 5m
  go: "1.24"

linters:
  default: none
  enable:
    - govet
    - ineffassign
    - unused

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - unused
