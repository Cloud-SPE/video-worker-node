version: "2"

run:
  timeout: 5m
  go: "1.25"

linters:
  default: none
  enable:
    - govet
    - ineffassign
    - unused
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - unused
